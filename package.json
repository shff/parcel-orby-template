{
  "license": "MIT",
  "scripts": {
    "serve": "parcel app/index.html --open",
    "build": "parcel build app/index.html --public-url .",
    "rebuild": "rimraf dist .cache && yarn build --no-cache"
  },
  "dependencies": {
    "@orby/core": "^0.1.7"
  },
  "devDependencies": {
    "parcel": "^2.0.1"
  },
  "browserslist": [
    ">0.25%",
    "not ie 11",
    "not op_mini all"
  ]
}
